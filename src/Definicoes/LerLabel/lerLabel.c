
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

#define TAM_MAX_LABEL 16

void lerLabel(char *linha){
    char *tmp = linha;
    char *al;

    while(linha =! ":")
    al == linha;
    print(al);

    free(*tmp);
    free(*al);
}